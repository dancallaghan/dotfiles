# Source zim
if [[ -s ${ZDOTDIR:-${HOME}}/.zim/init.zsh ]]; then
  source ${ZDOTDIR:-${HOME}}/.zim/init.zsh
fi

# Emacs mode
set -o emacs

# Set characters consider word boundaries
WORDCHARS='*?_-.[]~=&;!#$%^(){}<>'

# Local config
[[ -f ~/.zshrc.local ]] && source ~/.zshrc.local
